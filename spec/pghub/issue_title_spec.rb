require "spec_helper"

RSpec.describe Pghub::PostTitle do
  it "has a version number" do
    expect(Pghub::PostTitle::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
